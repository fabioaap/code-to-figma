# Plano Resumido

1. Validar build inicial (instalação OK, corrigir pacotes faltantes).
2. Corrigir pacote figma-plugin-lite (tipos e bundler) para permitir build.
3. Implementar pipeline de exportação no addon (captura HTML → conversão → Auto Layout).
4. Documentar métricas e processar exemplos adicionais.